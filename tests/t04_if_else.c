#include <stdio.h>
#include <stdlib.h>

int main(void) {
    int marks = 75;
    if ((marks >= 40)) {
        printf("%s\n", "Pass");
    } else {
        printf("%s\n", "Fail");
    }
    return 0;
}
